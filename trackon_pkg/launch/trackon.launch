<launch>
    <!-- Launch the image_processor_node -->
    <node
        pkg="trackon_pkg"
        type="trackon_node.py"
        name="trackon_service"
        output="screen">
        <rosparam file="$(find trackon_pkg)/config/config.yaml" command="load" />
    </node>
</launch>
